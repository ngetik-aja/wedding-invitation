name: Deploy Admin Dashboard

on:
  push:
    branches:
      - main
    paths:
      - "admin-dashboard/**"
      - ".github/workflows/deploy-admin-dashboard.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install flyctl
        run: |
          curl -L https://fly.io/install.sh | sh
          echo "$HOME/.fly/bin" >> "$GITHUB_PATH"

      - name: Deploy
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        working-directory: admin-dashboard
        run: |
          fly deploy -a dashboard-janjiakad -c fly.toml \
            --build-arg NEXT_PUBLIC_API_BASE_URL="https://janjiakad-api.fly.dev" \
            --build-arg NEXT_PUBLIC_PREVIEW_URL="https://janjiakad.fly.dev/preview"
